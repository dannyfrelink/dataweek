function Map() {
    function onClick(d) {
        const transportType = d.currentTarget.dataset.transport;
    }

    // SVG of Mickey's route
    return (
        <p>test</p>
    );
}

export default Map;